# Documentation (`docs/`)

This directory is intended for holding architectural decision records (ADRs), detailed domain documentation, user manuals, and API contracts.

Currently, architectural rules are driven by the `AGENTS.md` and OpenSpec specifications.
