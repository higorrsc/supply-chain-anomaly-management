# Utility Scripts (`scripts/`)

This directory contains utility scripts for database management, seeding, and development tooling.

## Key Scripts

- **`seed.py`**: Connects to the database and populates it with realistic dummy data (items, warehouses, movements, and anomalies) to facilitate local testing and dashboard development. Run via `uv run python scripts/seed.py`.
